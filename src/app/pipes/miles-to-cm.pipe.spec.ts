import { MilesToCmPipe } from './miles-to-cm.pipe';

describe('MilesToCmPipe', () => {
  it('create an instance', () => {
    const pipe = new MilesToCmPipe();
    expect(pipe).toBeTruthy();
  });
});
